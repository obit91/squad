---
"@bradygaster/squad-cli": patch
"@bradygaster/squad-sdk": patch
---

feat: add declarative plugin behavior context proof

Adds static plugin metadata, lifecycle state, runtime context injection, and
behavior simulations for Graphify, MemPalace, and Index Server plugin samples.
Enabled plugins can now contribute installed static guidance to spawned-agent
prompts while preserving the no-execution security boundary.
